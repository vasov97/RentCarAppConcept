import 'package:flutter/material.dart';

class AppIcons {
  static const Icon arrowForward = Icon(
    Icons.arrow_forward_ios,
    color: Colors.black,
  );

  static const Icon location = Icon(
    Icons.location_on,
    size: 60,
    color: Colors.red,
  );

  static const Icon star = Icon(
    Icons.star,
    color: Color.fromARGB(255, 240, 166, 29),
  );

  static const Icon direction = Icon(
    Icons.directions_car,
    color: Colors.white,
    size: 18,
  );
}
